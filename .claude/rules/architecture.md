---
globs:
  - "src/**"
---

<!--
  This rule loads ONLY when editing files in src/.
  Use it for: tech stack, component conventions, styling rules, routing.
  Without glob, it would load every session — wasting context.
-->

# Architecture

## Tech Stack
- Framework: [your framework]
- Styling: [your CSS approach]
- Components: [your component library]

## Routing
- [describe your routing conventions]

## Component Conventions
- [where components live, naming conventions, patterns]

## Styling Rules
- [design tokens, theme system, gotchas]

<!--
  TIP: Document "gotchas" here — things that break unexpectedly.
  Claude reads this before editing src/ files, so it avoids known pitfalls.
  Example: "Tailwind v4: hidden lg:grid doesn't work, use max-lg:hidden lg:grid"
-->
